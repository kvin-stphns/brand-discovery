# Contracts

Minimal setup for VoteWeight contract on Neon testnet (stub).

Commands:
- npm run contracts:compile
- npm run contracts:deploy:testnet

Outputs should write ABI/address to:
- frontend/lib/web3/abi.json
- backend/config/web3.json